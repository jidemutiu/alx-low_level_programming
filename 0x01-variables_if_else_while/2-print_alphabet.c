#include <stdio.h>
/**
 * main - program that prints the alphabet in lowercase.
 * Return: 0 (Always success)
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
		putchar('\n');
		return (0);
}
